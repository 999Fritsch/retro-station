# N64-specific overrides for RetroArch
# Workaround for mupen64plus-next black screen bug

# Use GL driver instead of Vulkan for N64 compatibility
video_driver = "gl"

# Start windowed (can toggle fullscreen with F key)
video_fullscreen = "false"
